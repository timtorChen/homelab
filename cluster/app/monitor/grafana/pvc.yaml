---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: monitor
  name: grafana
spec:
  storageClassName: proxmox-nvme
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100M
