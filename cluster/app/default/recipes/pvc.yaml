apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: mealie-config
spec:
  storageClassName: ceph-fs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1G
