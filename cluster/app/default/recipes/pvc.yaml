---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: mealie-data
spec:
  storageClassName: proxmox-nvme
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1G
