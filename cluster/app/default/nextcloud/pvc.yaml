---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: nextcloud-data
spec:
  storageClassName: proxmox-nvme
  resources:
    requests:
      storage: 250Gi
  accessModes:
    - ReadWriteMany
