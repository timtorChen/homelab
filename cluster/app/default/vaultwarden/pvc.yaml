apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-config
spec:
  storageClassName: proxmox-nvme
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi