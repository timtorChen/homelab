apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  namespace: default
  name: qbittorrent
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: qbittorrent-exporter
  endpoints:
    - port: metrics
