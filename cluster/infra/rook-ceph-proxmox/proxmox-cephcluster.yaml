---
apiVersion: ceph.rook.io/v1
kind: CephCluster
metadata:
  namespace: rook-ceph-proxmox
  name: rook-ceph-proxmox
spec:
  cephVersion:
    image: quay.io/ceph/ceph:v16.2.7
  dataDirHostPath: /ceph-proxmox
  external:
    enable: true
