---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  namespace: database
  name: redis-operator
spec:
  chart:
    spec:
      sourceRef:
        kind: HelmRepository
        namespace: flux-system
        name: ot-container
      chart: redis-operator
      version: 0.10.1
  interval: 1h
  values:
    redisOperator:
      imageName: timtor/redis-operator
      imageTag: v0.8.0