apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-hdd
provisioner: nfs.csi.k8s.io
parameters:
  server: 192.168.253.4
  share: /hdd-pool/garden
reclaimPolicy: Retain
volumeBindingMode: Immediate
mountOptions:
  - nconnect=8  # only supported on linux kernel version >= 5.3