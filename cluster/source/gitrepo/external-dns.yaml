---
apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  namespace: flux-system
  name: external-dns-crd-source
spec:
  interval: 5m
  url: https://github.com/kubernetes-sigs/external-dns
  ref:
    # yamllint disable rule:line-length
    # renovate: registryUrl=https://kubernetes-sigs.github.io/external-dns chart=external-dns
    tag: external-dns-helm-chart-1.9.0
    # yamllint enable
  ignore: |
    /*
    # include the crd file
    !/docs/contributing/crd-source/crd-manifest.yaml
