---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  namespace: flux-system
  name: redis-operator-crd
spec:
  interval: 10m
  prune: false
  sourceRef:
    kind: GitRepository
    name: redis-operator-crd-source
