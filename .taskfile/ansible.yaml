---
version: "3"

tasks:
  init:
    dir: provision/ansible
    cmds:
      - pip install -r requirements.txt --user
      - ansible-galaxy install -r galaxy.yml --force

  lint:
    dir: provision/ansible
    cmds:
      - ansible-lint
