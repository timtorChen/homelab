apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: home-assistant
  name: home-assistant-data
spec:
  storageClassName: rbd-fast
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteOnce
