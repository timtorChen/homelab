---
name: Helper
on:
  push:
    branches:
      - "action/config"
      - $default-branch
jobs:
  show-context:
    runs-on: ubuntu-latest
    steps:
      - name: Show context
        uses: crazy-max/ghaction-dump-context@v1